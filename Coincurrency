<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Deposit USDT (BSC)</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            text-align: center;
            background-color: #f1f1f1;
            margin: 0;
        }
        .container {
            max-width: 800px;
            margin: 50px auto;
            background: white;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }
        h1 {
            color: #007bff;
        }
        button {
            background-color: #007bff;
            color: white;
            padding: 12px 20px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            font-size: 16px;
        }
        button:hover {
            background-color: #0056b3;
        }
        .address-box {
            background-color: #f8f9fa;
            border: 1px solid #ddd;
            padding: 20px;
            margin-top: 20px;
            border-radius: 5px;
        }
    </style>
</head>
<body>

    <div class="container">
        <h1>Deposit USDT (BSC)</h1>
        <p>Click the button below to deposit USDT to our BSC address.</p>
        <button id="depositBtn">Deposit</button>
        <div class="address-box" id="depositAddress" style="display:none;">
            <h3>Deposit to the following address:</h3>
            <p><strong>0xfaafa18e407de3a280d42ddb04567f78045b23a9</strong></p>
        </div>
    </div>

    <script>
        // Show BSC Deposit Address when "Deposit" is clicked
        document.getElementById('depositBtn').addEventListener('click', function() {
            document.getElementById('depositAddress').style.display = 'block';
        });
    </script>

</body>
</html>
